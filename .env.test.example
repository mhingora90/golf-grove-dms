# Copy this file to .env.test and fill in the values.
# .env.test is gitignored — never commit actual keys.
#
# Get SUPABASE_SERVICE_KEY from:
#   Supabase dashboard → Settings → API Keys → Secret keys → default (click reveal)

SUPABASE_SERVICE_KEY=sb_secret_YOUR_KEY_HERE
